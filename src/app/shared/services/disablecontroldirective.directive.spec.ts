import { DisablecontroldirectiveDirective } from './disablecontroldirective.directive';

describe('DisablecontroldirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablecontroldirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
